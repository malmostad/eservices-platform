Ubuntu 13.10 and 14.04, 0.10.X versions are present. To install, just run:

sudo apt-get install nodejs
sudo apt-get install npm
sudo apt-get install nodejs-legacy

sudo npm install -g bower

Add the shared assets to your project with the following command:
bower install malmostad/shared_assets

To update the shared asset files, run:
bower update

sudo apt-get install ruby
sudo gem install sass

sass motrice.css.scss motrice.css

wag3: 
notera att nav taggen används i komin header vilket kolliderar med shared assets. (menyn blir två rader och skymmer huvudpanelen ifall man ändrar grid för nav)


